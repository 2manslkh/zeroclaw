[build]
dockerfilePath = "Dockerfile"
# Use the production distroless stage
buildTarget = "release"

[deploy]
startCommand = "zeroclaw gateway"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
